language: python
python:
  - "2.7"
  - "3.6"
install:
    - pip install -r requirements.txt
    - pip install -r requirements-test.txt
    - pip install paver
script: paver test
notifications:
  email: false
